spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: test
    password: root
    url: jdbc:mysql://localhost:3306/19_CRUD_API
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
